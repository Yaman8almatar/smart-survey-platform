from django.apps import AppConfig


class ResponsesConfig(AppConfig):
    """Configures the responses presentation app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.responses"
