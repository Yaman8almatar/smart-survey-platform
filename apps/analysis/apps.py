from django.apps import AppConfig


class AnalysisConfig(AppConfig):
    """Configures the analysis presentation app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.analysis"
