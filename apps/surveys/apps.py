from django.apps import AppConfig


class SurveysConfig(AppConfig):
    """Configures the surveys presentation app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.surveys"
